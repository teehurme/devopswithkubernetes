apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: project
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-server
            port:
              number: 1234